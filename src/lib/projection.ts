/**
 * @deprecated Import from '@/lib/selectors/projection' instead.
 * Re-export shim kept for backward compatibility during Phase 5 migration.
 */
export { computeProjection } from '@/lib/selectors/projection';
export type { ProjectionYear } from '@/lib/selectors/projection';
